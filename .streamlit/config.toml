[server]
address = "0.0.0.0"
port = 8501

[theme]
base = "light"  # light-first; native widgets (incl. the data-grid) render light
primaryColor = "#1455F5"  # BookPilot primary blue

[browser]
gatherUsageStats = false
