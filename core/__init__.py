"""Platform-level building blocks for the multi-tenant agent product:
tenant accounts (auth) and per-tenant data paths. Kept separate from `tracker/`
(the reused book engine) so the product plumbing never tangles with the analytics."""
